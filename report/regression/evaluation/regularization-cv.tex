Các mô hình Regularization có kết quả kém hơn rõ rệt, trung bình và độ lệch
chuẩn các lỗi tăng, khả năng giải thích giảm gần một nửa ($0.3288$, $0.1966$ và
$0.1377$ tương ứng với Ridge, Lasso và Elastic Net) so với OLS. Điều này càng cho
thấy dấu hiệu chưa khớp đã được thấy khi đánh giá trên tập kiểm tra.
